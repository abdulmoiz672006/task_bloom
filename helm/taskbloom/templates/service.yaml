apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskbloom.fullname" . }}
  labels:
    {{- include "taskbloom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskbloom.selectorLabels" . | nindent 4 }}
