apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taskbloom.fullname" . }}-test-connection"
  labels:
    {{- include "taskbloom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taskbloom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
