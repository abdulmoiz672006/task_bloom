apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: taskbloom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: moiz672006/taskbloom-backend:latest
        ports:
        - containerPort: 5000


