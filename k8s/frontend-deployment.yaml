apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: taskbloom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: moiz672006/taskbloom-frontend:latest
        ports:
        - containerPort: 3000

